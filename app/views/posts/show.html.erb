<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>
<%= image_tag(@post.avatar.thumb.url) if @post.avatar? %>
<p>
  <strong>Des:</strong>
  <%= @post.des %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
